# EatPrime — variáveis locais. NUNCA comitar este arquivo.
# Copie para ".env.local" (sem o .exemplo) e cole suas chaves.
# O .gitignore já bloqueia .env.local.

# Gemini (gratuito): https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Google Places (pago — o mesmo que você usa no coletar_dados.py)
GOOGLE_PLACES_API_KEY=
